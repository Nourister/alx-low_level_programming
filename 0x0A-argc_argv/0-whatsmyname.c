#include "main.h"
#include <stdio.h>

/**
 * main - that prints its name, followed by a new line.
 * @argc: counts number of arguments in a cmd line
 * @argv: size of an array of string
 * Return: 0 (success)
 */

int main(int argc, char *argv[])
{
	int argc;
	printf("%s\n", argv[0]);
	return (0);
}
