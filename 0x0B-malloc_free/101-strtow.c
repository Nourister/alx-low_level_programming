#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * strtow - splits a string into words.
 * @str: pointer to array of character
 * Return: NULL if str == NULL or str == "",
 * unction fails, it should return NULL,
 * a pointer to an array of strings (words)
 */

char **strtow(char *str)
{

